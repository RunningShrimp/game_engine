// Export both EntityTree variants
export { EntityTree } from './EntityTree';
export { VirtualEntityTree } from './VirtualEntityTree';

// VirtualEntityTree is the recommended version for large entity counts
// It uses react-window for efficient rendering with virtual scrolling
