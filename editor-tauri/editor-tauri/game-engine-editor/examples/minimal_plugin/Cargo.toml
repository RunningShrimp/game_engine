[package]
name = "minimal_plugin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
game-engine-editor = { path = "../../src-tauri" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
