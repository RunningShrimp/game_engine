[package]
name = "{{plugin-name}}"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
game-engine-editor = "0.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
