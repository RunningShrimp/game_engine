# TODO & Technical Debt Audit — Implementation Plan

Date: 2025-12-08

This document contains a concise, actionable implementation plan derived from a code-level audit for `game_engine` (source-only, not based on existing docs).

Summary
-------
- Found explicit in-code TODOs:
  - `src/domain/scene.rs` — TODO: consider moving validation into `try_new`
  - `src/services/tests.rs` — TODO: create a mock `GpuMesh` or choose another approach
- Also found high-risk patterns across the codebase: many `unwrap()`/`expect()` calls in runtime paths (not just tests), frequent use of `unsafe` in memory/arena code, and widespread `.lock().unwrap()` usage (risk of poisoning/panics).

High-level priorities
--------------------
- P0 (Immediate / Safety): Replace `unwrap()` or naked `lock().unwrap()` in runtime-critical modules (`src/network/**`, `src/performance/**`), audit `unsafe` modules and add safety tests + CI checks.
- P1 (High / Reliability): Make GPU-dependent tests mockable via traits/fakes; introduce result-returning APIs where constructors now permit invalid state (e.g., `Scene::try_new`).
- P2 (Medium): Reduce lock contention (fine-grained locks, sharded maps), add clippy/CI lints to prevent new unwraps in production code, and convert remaining TODOs into tracked issues.

Phase plan (Sprint-sized milestones)
-----------------------------------
Phase 0 (1 sprint) — Stabilize & Gate:
- Add CI guards to prevent new production unwraps.
- Hot-fix the top-N runtime unwraps in `src/network` and add tests.

Phase 1 (2 sprints) — Concurrency & Safety Audit:
- Audit `unsafe`/allocator modules, add targeted tests (MIRI/ASAN) where possible.
- Optimize lock usage in hotspots.

Phase 2 (2 sprints) — API & Testing Improvements:
- Move validation into `try_new` where necessary (start with `Scene`).
- Provide `GpuMesh` trait + mock implementations and change tests to use mocks.

Phase 3 (Ongoing) — Clean-up & Long-term Architecture:
- Convert TODOs -> issues -> tracked work across releases.
- Add static analysis gates, continuous testing for concurrency and memory bugs.

Quick Actions (first 2 weeks)
----------------------------
1) Auto-generate a P0 issue list from code hotspots (network unwraps, unsafe modules).  
2) Add CI checks to block committing unwrap/panic in non-test code.  
3) Fix the top `lock().unwrap()` and `parse().unwrap()` sites in `src/network/server.rs` and add tests.

Suggested owners
----------------
- Network safety: Senior backend/systems engineer  
- Unsafe/memory: Low-level systems engineer  
- GPU testing & render mocks: Render engineer  
- CI & QA: DevOps/CI engineer

Acceptance Criteria
-------------------
- No unhandled `unwrap()` in production paths for network/entry-point codebase.  
- `unsafe` modules have safety harness tests and a short audit report.  
- GPU-dependent tests use mocks and the test suite runs with stable CI.

Next steps
----------
Choose one of the following and I will start executing it now:

- Create per-item GitHub issues (P0/P1) with file/line references and fix suggestions.  
- Start a first fixing PR in `src/network/server.rs` to remove `lock().unwrap()` and add safe handling + tests.  
- Add CI/clippy changes to block new unwraps in production code.

---
Generated by an automated repository audit and analysis — ready to convert into issues/PRs.
